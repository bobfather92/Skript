{
  "name": "skript-desktop",
  "private": true,
  "scripts": {
    "e2e": "playwright test --config=playwright.config.cjs",
    "e2e:headed": "playwright test --config=playwright.config.cjs --headed",
    "e2e:debug": "playwright test --config=playwright.config.cjs --debug",
    "e2e:report": "playwright show-report test-results/e2e-report",
    "e2e:install": "playwright install chromium"
  },
  "devDependencies": {
    "playwright": "1.61.0"
  }
}
